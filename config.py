"""
전역 설정 값 모음.
"""
import os

# ── 모델 & 엔진 (실제 nano-vLLM 로드 대상) ──────────────────────────
MODEL_PATH = os.path.expanduser("~/huggingface/Qwen3-0.6B")
MAX_MODEL_LEN = 4096  # LONG_SYSTEM_PREFIX(~1500-2000토큰) + 본문 + 출력 토큰을 여유 있게 수용
GPU_MEMORY_UTILIZATION = 0.85
ENFORCE_EAGER = False  # False = CUDA Graph 사용 (참조자료의 CUDA Graph 최적화를 실측하기 위함)

# ── 워크로드 시나리오 ──────────────────────────────────────────────
# 모든 요청에 공통으로 붙는 시스템 프롬프트(Prefix).
#
# 실제 nano-vLLM의 KV 캐시 블록 크기(kvcache_block_size)는 256 토큰 고정이고
# (nanovllm/config.py: assert kvcache_block_size % 256 == 0, 기본값 256),
# BlockManager는 '완전히 채워진 블록'만 해시로 캐시 재사용한다
# (block_manager.py can_allocate: range(seq.num_blocks - 1) — 마지막 블록은 제외).
# 즉 SYSTEM_PREFIX가 256 토큰보다 짧으면 실제 엔진에서는 단 한 번도 프리픽스
# 캐시가 히트하지 않는다. 아래 지침 프롬프트는 Qwen3 토크나이저 기준 약 370
# 토큰이라, 항상 최소 1개 블록(256토큰) 전체가 프리픽스로만 채워져
# 이메일 본문 내용과 무관하게 캐시 재사용이 보장된다.
SYSTEM_PREFIX = """당신은 사내 이메일에서 업무 제출 기한과 제출물을 정확히 추출하는 어시스턴트입니다.
아래 지침을 반드시 지켜서 답변하세요.

[추출 규칙]
1. 이메일 본문에서 "제출해야 하는 대상(문서/자료/양식 이름)"과 "제출 기한(날짜 또는 요일, 시각)"을 각각 식별합니다.
2. 제출 기한이 여러 개 언급된 경우, 가장 마지막에 명시된 공식 기한을 우선합니다.
3. 기한 표현이 상대적인 경우(예: "이번 주 금요일", "다음 주 월요일") 원문 표현을 그대로 유지하고 별도로 추정하지 않습니다.
4. 제출 대상이 명확하지 않으면 "미상"으로 표기합니다.
5. 반드시 아래 형식을 그대로 따라 출력합니다.

[출력 형식]
제출물: <제출 대상>
기한: <제출 기한>
비고: <추가로 참고할 사항이 있다면 한 문장으로 요약, 없으면 "없음">

[예시]
입력: "안녕하세요, 2분기 실적 보고서를 이번 주 금요일 18:00까지 제출 부탁드립니다."
출력:
제출물: 2분기 실적 보고서
기한: 이번 주 금요일 18:00
비고: 없음

이제 다음 메일에서 언제까지, 무엇을 제출해야 하는지 위 형식에 맞춰 추출해 줘:

"""

# Prefix Caching의 TTFT 효과는 "캐시로 재사용하는 토큰 수"와 "새로 계산해야 하는
# 토큰 수"의 비율이 클수록 뚜렷해진다. SYSTEM_PREFIX(약 370토큰, 1블록)는 실제
# 이메일 본문(약 60~90토큰)에 비해 재계산량 차이가 작아서(179~194 vs 435~450
# 토큰), Qwen3-0.6B처럼 작은 모델에서는 그 몇백 토큰 차이가 만드는 실제 연산
# 시간이 스케줄러/커널 실행 고정 오버헤드(~20ms대)에 묻혀 TTFT 차이가 거의
# 안 보인다. LONG_SYSTEM_PREFIX는 few-shot 예시를 7개로 늘려 약 5~6블록
# (1500~2000토큰)을 캐시 대상으로 만든다 — RAG 컨텍스트나 few-shot이 많은
# 실제 프로덕션 시스템 프롬프트 규모를 흉내낸 것으로, 캐시 히트 시 재계산량이
# 캐시 미스 대비 훨씬 작아져(본문 토큰만) 고정 오버헤드를 뚫고 TTFT 차이가
# 드러나는지 확인하기 위한 스트레스 테스트용 프리픽스다.
LONG_SYSTEM_PREFIX = SYSTEM_PREFIX.replace(
    "이제 다음 메일에서 언제까지, 무엇을 제출해야 하는지 위 형식에 맞춰 추출해 줘:",
    """[추가 예시 1]
입력: "안녕하세요, 정보보안 서약서를 다음 주 월요일 오전 9시까지 작성해서 제출해 주세요."
출력:
제출물: 정보보안 서약서
기한: 다음 주 월요일 오전 9시
비고: 없음

[추가 예시 2]
입력: "수고 많으십니다. 법인카드 사용 내역 정산은 이번 달 25일 자정까지 완료 부탁드립니다. 관련 문의는 내선 1234로 연락 주세요."
출력:
제출물: 법인카드 사용 내역 정산
기한: 이번 달 25일 자정
비고: 문의는 내선 1234

[추가 예시 3]
입력: "안녕하세요, 항상 업무 협조에 감사드립니다. 연차 사용 계획서를 다음 주 수요일까지 제출해 주시기 바랍니다."
출력:
제출물: 연차 사용 계획서
기한: 다음 주 수요일까지
비고: 없음

[추가 예시 4]
입력: "사내 교육 이수 확인증은 이번 달 말일까지 제출해야 하며, 첨부된 양식을 다운로드하여 작성 후 회신해 주시기 바랍니다."
출력:
제출물: 사내 교육 이수 확인증
기한: 이번 달 말일
비고: 첨부 양식 작성 후 회신

[추가 예시 5]
입력: "안녕하십니까, 협력사 계약서 검토 의견은 내일 오후 3시까지 회신 부탁드리며, 부서장 결재가 필요한 항목이니 일정을 확인해 주세요."
출력:
제출물: 협력사 계약서 검토 의견
기한: 내일 오후 3시
비고: 부서장 결재 필요

[추가 예시 6]
입력: "출장 경비 영수증은 복귀 후 3영업일 이내에 제출해 주시기 바랍니다. 감사합니다."
출력:
제출물: 출장 경비 영수증
기한: 복귀 후 3영업일 이내
비고: 없음

[추가 예시 7]
입력: "개인정보 처리 현황 점검표를 다음 주 금요일 17:00까지 제출해 주시기 바랍니다. 본 안내는 전 직원 대상 공통 공지사항입니다."
출력:
제출물: 개인정보 처리 현황 점검표
기한: 다음 주 금요일 17:00
비고: 전 직원 공통 공지

이제 다음 메일에서 언제까지, 무엇을 제출해야 하는지 위 형식에 맞춰 추출해 줘:""",
)

# ── GPU 프로파일링 데몬 (실제 nvidia-smi 폴링) ──────────────────────
GPU_POLL_INTERVAL_S = 0.5   # 폴링 주기 (초)
GPU_HISTORY_MAXLEN = 600    # 히스토리 보관 개수 (0.5s * 600 = 5분)
